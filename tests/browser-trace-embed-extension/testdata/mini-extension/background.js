// Minimal service worker stub for fixture MV3 package.
// Product embed may replace this tree via script/browser-trace/bundle.
// CI tests must not require webpack/npm — a staged mini-extension is enough.
console.log("browser-trace fixture extension loaded");
